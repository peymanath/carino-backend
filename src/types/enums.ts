export enum Gender {
  MALE = 1,
  FEMALE = 2,
  MIXED = 3,
}

export enum EnumBrowserEngine {
  WEBKIT,
  BLINK,
  GECKO,
  TRIDENT,
  OTHER,
}

export enum EnumBrowserName {
  CHROME,
  FIREFOX,
  SAFARI,
  EDGE,
  OPERA,
  OTHER,
}

export enum EnumDeviceType {
  DESKTOP,
  MOBILE,
  TABLET,
  BOT,
  OTHER,
}

export enum EnumOSName {
  WINDOWS,
  MACOS,
  LINUX,
  ANDROID,
  IOS,
  OTHER,
}

export enum CredentialDeviceType {
  SINGLE_DEVICE = 0,
  MULTI_DEVICE = 1,
}

export enum CredentialBackupStatus {
  NOT_BACKED_UP = 0,
  BACKED_UP = 1,
}
